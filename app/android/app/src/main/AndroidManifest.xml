<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Required for every real HTTP call the app makes (auth, papers, payments, ...).
         Missing entirely before 2026-08-28 — never caught because every test this app
         had run through was web (no Android permission model) or a mocked HTTP client;
         a real device install just silently blocked all network access, which is why
         splash worked but nothing past it ever loaded real data. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- cunning_document_scanner (2026-09-01) accesses the camera directly
         for live edge detection while scanning a paper — unlike the plain
         image_picker camera flow elsewhere in this app, which delegates to
         the system camera app via an implicit intent and never needed this
         permission declared. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <application
        android:label="spekooh"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- AdMob app ID, required by google_mobile_ads regardless of which ad unit is
             requested — see app/lib/ads/ad_ids.dart for the ad unit IDs themselves. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-6272769995522353~8764029955" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
