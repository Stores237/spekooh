Switch used for quiz-setup options (Timer, Hints, Shuffle questions). On = green-500 fill; off = light gray track. No text label inside — label sits to its left in a ListItemRow-style layout.

```jsx
<Toggle checked={timerOn} onChange={setTimerOn}/>
```
