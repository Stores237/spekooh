Uppercase pill label for metadata tags — "MOCK", "PAPER 2", "TRENDING", "SPONSORED", "ACCOUNTING".

```jsx
<Badge tone="amber">Mock</Badge>
<Badge tone="neutral">Paper 2</Badge>
```

Tones: `blue` default, `amber` promo/trending, `green` positive/verified, `neutral` gray filter chip, `dark` solid on light backgrounds.
