Circular avatar, used on the "Top players" leaderboard. Falls back to initials on a blue tint when no image. Rank 1 gets a small star above and an amber ring instead of white.

```jsx
<Avatar name="Jojo B." rank={1}/>
<Avatar name="Julliete" rank={2}/>
```
